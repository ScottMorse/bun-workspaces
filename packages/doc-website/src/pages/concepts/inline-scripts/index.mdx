---
title: Concepts > Inline Scripts
titleSuffix: " | bun-workspaces"
description: "Inline Scripts | Concepts | Documentation for bun-workspaces: A CLI and API to help manage Bun monorepos"
pageType: doc
sidebar: false
outline: true
footer: false
---
import { exampleWorkspaceConfigSimple } from '../../../lib/config/exampleConfig'
import { SyntaxHighlighter } from '../../../lib/util/highlight';
import { ConceptsHeader } from "../../../lib/concepts/ConceptsHeader";
import { getUserEnvVarName } from 'bun-workspaces/src/config/userEnvVars';
import { CLI_INLINE_NAME_EXAMPLE, CLI_INLINE_SHELL_EXAMPLE } from '../../../lib/cli/cliExamples';
import { API_INLINE_NAME_EXAMPLE, API_INLINE_SHELL_EXAMPLE } from '../../../lib/api/apiExamples';

<ConceptsHeader activeHref="inlineScripts" />

Normally, a ["script"](/concepts/glossary#script) in bun-workspaces references one of the entries found in a workspace's `"scripts"` field in its `package.json` file.

However, it's possible to run a one-off shell command from a workspace's directory, an "inline script", such as via `bw run "echo 'hello'" --inline` in the CLI.

## Shell

Normally, inline scripts are executed using the [Bun shell](https://bun.com/docs/runtime/shell), a bash-like, cross-platform shell, the same as the utility `import { $ } from 'bun'`.

You can pass `--shell=system` to use the native shell instead. In Unix (Linux, macOS, etc.), `sh -c` is called, while in Windows, `cmd /d /s /c` is called.

You can also set the environment variable <code>{getUserEnvVarName("scriptShellDefault")}</code> to `system` to use the native shell by default.

### Examples

#### CLI:
<SyntaxHighlighter language="bash">
{CLI_INLINE_SHELL_EXAMPLE}
</SyntaxHighlighter>

#### API:
<SyntaxHighlighter language="typescript">
{API_INLINE_SHELL_EXAMPLE}
</SyntaxHighlighter>

## Script Name

Scripts from `package.json` are named by the key in the `"scripts"` field. 

This is used in output logs and [script runtime metadata](/concepts/script-runtime-metadata).

Inline scripts, however, have no name by default, but you can provide a name.

### Examples

#### CLI:
<SyntaxHighlighter language="bash">
{CLI_INLINE_NAME_EXAMPLE}
</SyntaxHighlighter>

#### API:
<SyntaxHighlighter language="typescript">
{API_INLINE_NAME_EXAMPLE}
</SyntaxHighlighter>