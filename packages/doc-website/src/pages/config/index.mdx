---
title: Configuration
titleSuffix: " | bun-workspaces"
description: "Configuration documentation for bun-workspaces, such as the bw.json config file | Documentation for bun-workspaces: A CLI and API to help manage Bun monorepos"
pageType: doc
sidebar: false
outline: true
footer: false
---

import { ROOT_CONFIG_FILE_NAME, ROOT_CONFIG_PACKAGE_JSON_KEY } from "bun-workspaces/src/config";
import { exampleDeprecatedConfig, exampleWorkspaceConfigArray, exampleWorkspaceConfigSimple, createPackageJsonExample } from "../../lib/cli/exampleConfig";
import { SyntaxHighlighter } from "../../lib/util/highlight";
// import { WORKSPACE_CONFIG_FILE_PATH, WORKSPACE_CONFIG_PACKAGE_JSON_KEY} from 'bun-workspaces/src/config/workspaceConfig/workspaceConfigLocation'
import { CONFIG_LOCATION_TYPES, createConfigLocationPath } from 'bun-workspaces/src/config/util'
import { ROOT_CONFIG_TYPE } from "../../lib/config/configTypes";

# Configuration

## Precedence (Resolution Order)

Values related to configuration are resolved in the following order of precedence:
1. An explicit CLI or API argument/option passed to a method/function/command
2. A value provided in a configuration file
3. A value provided via an environment variable, if applicable
4. The generic default value, if applicable

## Project Root Configuration

The root configuration is the general configuration for a project.
It is read from the following locations, in order of precedence:
<ul className="config-location-list">
{CONFIG_LOCATION_TYPES.map(type => <li key={type}><code>{createConfigLocationPath(type, ROOT_CONFIG_FILE_NAME, ROOT_CONFIG_PACKAGE_JSON_KEY)}</code></li>)}
</ul>

### Type Definition

<SyntaxHighlighter language="typescript">
  {ROOT_CONFIG_TYPE}
</SyntaxHighlighter>

## Workspace Configuration

{/* You can add optional configuration to a workspace in its `package.json` file in  <code>"{WORKSPACE_CONFIG_PACKAGE_JSON_KEY}"</code> 
or in a separate <code>{WORKSPACE_CONFIG_FILE_PATH}</code> file. */}

See [workspace aliases](/concepts/workspace-aliases) and [script execution order](/concepts/script-execution-order) for more information on the fields available.

### Examples:
#### 1. Separate Config File
{/* <p className="workspace-config-path"><code>path/to/your/workspace/{WORKSPACE_CONFIG_FILE_PATH}</code></p> */}

<SyntaxHighlighter language="json">
  {JSON.stringify(exampleWorkspaceConfigSimple, null, 2)}
</SyntaxHighlighter>

#### 2. Within package.json
<p className="workspace-config-path"><code>path/to/your/workspace/package.json</code></p>

{/* The same configuration can be added to the workspace's `package.json` file in <code>"{WORKSPACE_CONFIG_PACKAGE_JSON_KEY}"</code>. */}
<SyntaxHighlighter language="json">
  {JSON.stringify(createPackageJsonExample(exampleWorkspaceConfigSimple), null, 2)}
</SyntaxHighlighter>

{/* Note that if both package.json config and <code>{WORKSPACE_CONFIG_FILE_PATH}</code> are present,  */}
{/* <code>{WORKSPACE_CONFIG_FILE_PATH}</code> will take precedence. It's recommended to only use one type of configuration within your */}
project for consistency.

## Config File (bw.json)

<div className="config-deprecation-text">
DEPRECATED: Use the new workspace configuration above instead to configure workspace aliases. 

For the meantime, this
configuration file will still take effect and merge aliases with the new configuration if present.
</div>

You can create a config file at `bw.json` in your project root,
or you can pass a config file to the CLI with the `--config-file` (or `-c`) [option](/cli#cli-global-option-configFile).

#### Example config

In this config, "app-a" is an alias for package "@my-org/application-a"
and "app-b" is an alias for package "@my-org/application-b".

The default
[log level](/cli#cli-global-option-logLevel) can be set for the CLI as well.

<SyntaxHighlighter language="json">
  {JSON.stringify(exampleDeprecatedConfig, null, 2)}
</SyntaxHighlighter>
