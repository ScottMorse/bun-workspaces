---
pageType: doc
sidebar: false
outline: false
footer: false
---

import packageJson from "../../../bun-workspaces/package.json";
import { Bwunster } from "../content/Bwunster";
import { NormalizeIndex } from "../content/NormalizeIndex";
import { SyntaxHighlighter } from "../content/highlight";

<Bwunster />

<NormalizeIndex />

<h1 className="home-title">bun-workspaces</h1>
<p className="home-version">v{packageJson.version}</p>

<div className="home-subtitle">
  A CLI on top of native [Bun
  workspaces](https://bun.sh/docs/install/workspaces)
</div>
<div className="home-list-container">
  <ul className="home-list">
    <li>Get metadata about your project 🤖</li>
    <li>Run scripts across your workspaces 🧾</li>
    <li>Works right away, with no other setup required 🍔🍴</li>
  </ul>
</div>

## <span className="whats-new-title">What's New</span>

<h2 className="whats-new-subtitle">Notable recent updates 📦</h2>

- Added
  `--json-outfile` option to `run-script` to output results ([more
  info](/cli#json-output))

## Installation

You can install the [CLI](/cli) in your project or simply use `bunx bun-workspaces`.

<SyntaxHighlighter language="bash">
{`# Install to lock the version used in your project
bun add --dev bun-workspaces

# Start using the CLI with or without the installation step

bunx bun-workspaces --help`}

</SyntaxHighlighter>

If you use the CLI frequently, you might consider using a shell alias.

<SyntaxHighlighter language="bash">
  {`# Add this to .bashrc, .zshrc, or similar 
# to make it available by default in your shell
alias bw="bunx bun-workspaces"`}
</SyntaxHighlighter>

Examples in these docs will use the alias `bw` for `bunx bun-workspaces` for brevity.

### Config

[See here](/config) for more information on the optional `bw.json` file, where you can
configure aliases for workspaces.

## Quick Start

[See the full CLI documentation](/cli) or continue for some quick examples.

<SyntaxHighlighter language="bash">
{`# List all workspaces in your project
# based on the "workspaces" field in
# your root package.json file
bw list-workspaces

# ls is an alias for list-workspaces

# This prints formatted JSON

bw ls --json --pretty

# Run the lint script for all workspaces

# that have it in their "scripts" field

bw run lint

# Run the lint script for a specific workspace

bw run lint my-workspace

# Run the lint script for multiple workspaces

bw run lint my-workspace-a my-workspace-b

# Run the lint script for workspaces using wildcard

bw run lint "my-workspace-\*"

# Run the lint script in parallel

bw run lint --parallel

# Append args to each script call

bw run lint --args="--my --appended --args"

# Use the workspace name in args

bw run lint --args="--my --arg=<workspace>"

# Help

bw help
bw --help

# Pass --cwd to any command

bw --cwd=/path/to/your/project ls
bw --cwd=/path/to/your/project run my-script

# Pass --config-file to any command

bw --config-file=/path/to/your/config.json ls

# Pass --log-level to any command (debug, info, warn, error, or silent)

bw --log-level=silent run my-script`}

</SyntaxHighlighter>
