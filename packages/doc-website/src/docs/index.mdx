---
pageType: doc
sidebar: false
outline: false
footer: false
---

import packageJson from "../../../bun-workspaces/package.json";
import { Bwunster } from "../content/Bwunster";
import { NormalizeIndex } from "../content/NormalizeIndex";
import { SyntaxHighlighter } from "../content/highlight";
import { BragTicker } from "../content/BragTicker";
import { API_QUICKSTART } from '../content/api/apiExamples';
import { CLI_QUICKSTART } from '../content/cli/quickstart';

<Bwunster />

<NormalizeIndex />

<h1 className="home-title">bun-workspaces<br/><span className="home-subtitle">
  A CLI on top of native [Bun
  workspaces](https://bun.sh/docs/install/workspaces)</span></h1>
<p className="home-version">v{packageJson.version}</p>

<div className="home-list-container">
  <ul className="home-list">
    <li>Get metadata about your project 🤖</li>
    <li>Run scripts across your workspaces flexibly 🧾</li>
    <li>Works right away, with no boilerplate required 🍔🍴</li>
  </ul>
</div>

<hr />

<h2 className="whats-new-title">What's New</h2>

<h2 className="whats-new-subtitle">Notable recent updates 📦</h2>
<div className="whats-new-list">
- An API has been made official! 🛠️️ [Go see the documentation here](/api)
  - If you are sneaky and found the previous experimental exports, this will be a breaking change for you.
  - Now that the API has been officially released, stability will be maintained more rigorously.
- New [workspace configuration](/config) is supported in either package.json or a separate config file.
  - The root `bw.json` file is now <span className="deprecated">deprecated</span> ([more info](/config#config-file-bwjson))
- Added
  `--json-outfile` option to `run-script` to output results ([more
  info](/cli#json-output))
</div>

## Installation

You can install the package in your project or simply use `bunx bun-workspaces` to immediately use the latest version of the [CLI](/cli). Installation is required to use the [API](/api).

<SyntaxHighlighter language="bash">
{`# Install to use the API and/or lock your CLI version for your project
bun add --dev bun-workspaces

# Start using the CLI with or without the installation step
bunx bun-workspaces --help`}

</SyntaxHighlighter>

If you use the CLI frequently, you might consider using a shell alias.

<SyntaxHighlighter language="bash">
  {`# Add this to .bashrc, .zshrc, or similar 
# to make it available by default in your shell
alias bw="bunx bun-workspaces"`}
</SyntaxHighlighter>

Examples in these docs will use the alias `bw` for `bunx bun-workspaces` for brevity.

## Quick Start

#### CLI

[See the full CLI documentation](/cli) or continue for some quick examples.

<SyntaxHighlighter language="bash">
{CLI_QUICKSTART}

</SyntaxHighlighter>

#### API

[See the full API documentation](/api) or continue for some quick examples.

<SyntaxHighlighter language="typescript">
{API_QUICKSTART}
</SyntaxHighlighter>

<br/>
_`bun-workspaces` is independent from the [Bun](https://bun.sh) project and is not affiliated with or endorsed by Oven. This project aims to enhance enhance the experience of Bun for its users._

<BragTicker />