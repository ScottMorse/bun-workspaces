{
  "name": "bun-workspaces",
  "version": "1.0.0-alpha.26",
  "main": "src/index.ts",
  "private": true,
  "homepage": "https://bunworkspaces.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/bun-workspaces/bun-workspaces.git"
  },
  "bin": {
    "bun-workspaces": "bin/cli.js",
    "bw": "bin/cli.js"
  },
  "_bwInternal": {
    "bunVersion": {
      "build": "1.3.4",
      "libraryConsumer": "^1.2.x"
    }
  },
  "scripts": {
    "cli": "bun --silent run bin/cli.js",
    "cli:dev": "_BW_RUNTIME_MODE=development bun --silent run bin/cli.js",
    "lint": "eslint src",
    "build": "bun run scripts/build.ts",
    "build:test": "BUILD_INCLUDE_TESTS=true bun run scripts/build.ts && chmod +x dist.test/bin/cli.js && cd dist.test && bun install && IS_BUILD=true bun test && cd .. && rm -rf dist.test",
    "build:rslib-only": "rslib build",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@rslib/core": "^0.15.1",
    "bun-workspaces": "file:."
  },
  "dependencies": {
    "commander": "^12.1.0"
  }
}
